import React from 'react';

import Button from 'components/Button/Button';

const RoundedButton = props => {
  return <Button className="ui-btn--rounded" {...props} />;
};

export default RoundedButton;
